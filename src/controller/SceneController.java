package controller;

import javafx.stage.Stage;

public interface SceneController {
    // Abstraction: Defines a template that all controllers must follow [cite: 21, 47]
    void setStage(Stage stage);
}